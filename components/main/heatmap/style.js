import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  scrollViewContent: {
    padding: 20,
    marginTop: 10,
  },
  textViewContainer:{
    flex:1,
    alignItems:'center',
    justifyContent:'center',
  },
  itemText:{
    color:'white'
  }
});
