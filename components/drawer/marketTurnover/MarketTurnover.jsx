import { View } from "react-native"
import { Text } from "react-native-paper";

export const MarketTurnOver = ()=>{
    return (
            <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
              <Text>indices!</Text>
            </View>
          );
}